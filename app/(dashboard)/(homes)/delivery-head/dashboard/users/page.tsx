// Delivery Head Users Management page — reuses the same admin users component.
// Placed under the delivery-head route to avoid any admin-route access guards.
export { default } from "@/app/(dashboard)/(homes)/admin/dashboard/users/page";
